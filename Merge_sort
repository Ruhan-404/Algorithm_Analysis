#include<bits/stdc++.h>
using namespace std;


void insertion_sort(vector<int>&v,int n)
{
	for(int i=1;i<n;i++)
	{
		int key = v[i],j;
		for( j=i-1 ;j>=0;j--)
		{
			if(v[j]>key)
				v[j+1]=v[j];
			else break;
		}
		v[j+1]=key;
	}
}

void merge(vector<int>&v,int l,int m, int r)
{
	vector<int>temp;
	for(int i=l,j=m+1,k=l;k<=r;k++)
	{
		if(i>m)temp.push_back(v[j++]);
		else if(j>r)temp.push_back(v[i++]);
		else if(v[i]>v[j])temp.push_back(v[j++]);
		else temp.push_back(v[i++]);

	}
	for(int i=l;i<=r;i++) v[i] = temp[i-l];
}

void merge_sort(vector<int>&v,int l,int r)
{
	if(l>=r)return;
	int m = (l+r)/2;

	merge_sort(v,l,m);
	merge_sort(v,m+1,r);
	merge(v,l,m,r);
}

main()
{
	int n;
	cin>>n;
	vector<int>v(n);
	for(int i=0;i<n;i++)cin>>v[i];
	merge_sort(v,0,n-1);
	for(auto i : v)cout<<i<<" ";
		cout<<endl;
}
